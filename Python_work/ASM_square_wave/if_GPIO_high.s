@Depending on the GPIO's state when the code ends, 
@the pin may remain in a high state. Create another '.s' file that
@places the pin in a low state and have it run after the square wave output ends.